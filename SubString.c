#include <stdio.h>

int main(){

    char P[100];
    char T[100];
    int i,j;

    scanf("%s",T);
    scanf("%s",P);

    for(i=0;T[i]!='\0';i++){
        for(j=0;P[j]!='\0';j++){
            if(T[i]==P[j]){
                    
            }else{

            }
        }
    }






    return 0;
}